# base path to the pmem file; service appends '.N' to the path (where N is localid)
path = /mnt/fake_pmem/service

# pmem poll size (256 * 1024 * 1024)
# request size * key space is 4096bytes*10000 = 40,96 megabytes
size = 268435456
